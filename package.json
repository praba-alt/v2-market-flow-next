{
  "name": "v2-market-flow-next",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "cron:sync:local": "node scripts/run-cron-step.mjs sync",
    "cron:enrich:local": "node scripts/run-cron-step.mjs enrich",
    "cron:run:local": "node scripts/run-cron-after-build.mjs",
    "abis:collect": "node scripts/collect-relevant-abis.mjs",
    "build:cron": "npm run build && npm run cron:run:local",
    "build:cron:sync": "npm run build && npm run cron:sync:local",
    "build:cron:enrich": "npm run build && npm run cron:enrich:local"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "ethers": "^6.15.0",
    "next": "14.2.5",
    "puppeteer": "^24.8.2",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
